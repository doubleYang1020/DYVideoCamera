use_frameworks!

platform :ios, '8.0'

target 'DYVideoCamera_Example' do
  pod 'DYVideoCamera', :path => '../'

  target 'DYVideoCamera_Tests' do
    inherit! :search_paths

    pod 'Specta'
    pod 'Expecta'
    pod 'FBSnapshotTestCase'
    pod 'Expecta+Snapshots'
  end
end
